<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>Welcome!</title>
        <link rel="stylesheet" type="text/css" href="{{ "css/styles.css" }}">
    </head>
    <body>
        <div class="container main-container">

        </div>
        <div class="container">
            <input type="file" id="file-selector" name="file-selector">
            <input type="submit" value="Submit" onclick="submitForm('{{ path("upload") }}')">
        </div>

        <p class="container error" id="error"></p>
    </body>
    <script type="text/javascript" src="{{ "js/scripts.js" }}"></script>
</html>
